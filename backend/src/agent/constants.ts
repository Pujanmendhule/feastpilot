/** Default restaurant when no session selection exists (see mock/restaurants.json). */
export const DEFAULT_RESTAURANT_ID = "behrouz_biryani";

/** Default menu item for cart flows (see mock/menus.json). */
export const DEFAULT_MENU_ITEM_ID = "bb_3";

/** Default quantity when adding items to cart. */
export const DEFAULT_QUANTITY = 1;
